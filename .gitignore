# Views GENERATED by scripts/render_state.py.
# Deliberately not committed: that is what keeps two branches from editing the
# same state file. Commit them and the conflicts come back.
docs/agent/STATE.md
docs/agent/log/INDEX.md
